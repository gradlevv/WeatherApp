package com.example.admin.weatherui.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "HOUR".
*/
public class HourDao extends AbstractDao<Hour, Long> {

    public static final String TABLENAME = "HOUR";

    /**
     * Properties of entity Hour.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property City_name = new Property(1, String.class, "city_name", false, "CITY_NAME");
        public final static Property Lon = new Property(2, double.class, "lon", false, "LON");
        public final static Property Timezone = new Property(3, String.class, "timezone", false, "TIMEZONE");
        public final static Property Lat = new Property(4, double.class, "lat", false, "LAT");
        public final static Property Country_code = new Property(5, String.class, "country_code", false, "COUNTRY_CODE");
        public final static Property State_code = new Property(6, String.class, "state_code", false, "STATE_CODE");
    }

    private DaoSession daoSession;


    public HourDao(DaoConfig config) {
        super(config);
    }
    
    public HourDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"HOUR\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"CITY_NAME\" TEXT," + // 1: city_name
                "\"LON\" REAL NOT NULL ," + // 2: lon
                "\"TIMEZONE\" TEXT," + // 3: timezone
                "\"LAT\" REAL NOT NULL ," + // 4: lat
                "\"COUNTRY_CODE\" TEXT," + // 5: country_code
                "\"STATE_CODE\" TEXT);"); // 6: state_code
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"HOUR\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Hour entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String city_name = entity.getCity_name();
        if (city_name != null) {
            stmt.bindString(2, city_name);
        }
        stmt.bindDouble(3, entity.getLon());
 
        String timezone = entity.getTimezone();
        if (timezone != null) {
            stmt.bindString(4, timezone);
        }
        stmt.bindDouble(5, entity.getLat());
 
        String country_code = entity.getCountry_code();
        if (country_code != null) {
            stmt.bindString(6, country_code);
        }
 
        String state_code = entity.getState_code();
        if (state_code != null) {
            stmt.bindString(7, state_code);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Hour entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String city_name = entity.getCity_name();
        if (city_name != null) {
            stmt.bindString(2, city_name);
        }
        stmt.bindDouble(3, entity.getLon());
 
        String timezone = entity.getTimezone();
        if (timezone != null) {
            stmt.bindString(4, timezone);
        }
        stmt.bindDouble(5, entity.getLat());
 
        String country_code = entity.getCountry_code();
        if (country_code != null) {
            stmt.bindString(6, country_code);
        }
 
        String state_code = entity.getState_code();
        if (state_code != null) {
            stmt.bindString(7, state_code);
        }
    }

    @Override
    protected final void attachEntity(Hour entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Hour readEntity(Cursor cursor, int offset) {
        Hour entity = new Hour( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // city_name
            cursor.getDouble(offset + 2), // lon
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // timezone
            cursor.getDouble(offset + 4), // lat
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // country_code
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6) // state_code
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Hour entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCity_name(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setLon(cursor.getDouble(offset + 2));
        entity.setTimezone(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setLat(cursor.getDouble(offset + 4));
        entity.setCountry_code(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setState_code(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Hour entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Hour entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Hour entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
